# Sesión 2026-09-06 08:20 UTC

1. Qué cambió: fase 3 completa — nuevo `app/` con backend Rust (axum + tokio, `GET /` en texto plano y `GET /salud` con el `BUILD_ID`), `app/Dockerfile` multi-stage, `app/fly.toml` de la app `desdemovil-npi` y `.github/workflows/deploy.yml` que crea la app, fija `BUILD_ID=$GITHUB_SHA`, despliega con flyctl y verifica `/salud` con hasta 10 reintentos.
2. SHA: `798fccd` (fase 3) y `9086f77` (cambio de región) en `main`; run [34021419513](https://github.com/npiobject/DesdeMovil/actions/runs/34021419513) en `success` confirmado por la API de Actions.
3. URL para probar: https://desdemovil-npi.fly.dev/ y https://desdemovil-npi.fly.dev/salud — devuelve `{"build":"9086f77abe96f1e9da60b6ccedc6a17aae68c04f","ok":true}`; `docs/` sigue en `build=DM-B3-20260905-001`.
4. Resultado en Drive: subidos `fase3-fly.md` y este resumen en `sesiones/`, sin borrar nada existente.
5. Qué falta: confirmar si `cdg` es la región definitiva (Fly ha deprecado `mad` y no admite recursos nuevos allí), dominio propio, y conectar el mock de `docs/` con el backend — hoy son dos despliegues independientes.
