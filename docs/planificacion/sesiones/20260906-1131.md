# Sesión 2026-09-06 11:31 — inicialización desde la plantilla

1. Qué cambió: la inicialización automática no se había ejecutado (no había `.plantilla-pendiente`, pero el commit inicial llegó a `main` con el nombre `DesdeMovil` en los diez sitios `PLANTILLA:`). Sustituido a `proyecto_2` en `README.md`, `CLAUDE.md`, `app/fly.toml` + `FLY_APP` (`proyecto-2-npi`), `Cargo.toml`/`Cargo.lock`/`Dockerfile` (`proyecto-2-backend`), `tools/*.ps1` y `docs/index.html` (build `P2-B3-20260906-001`); comentarios `PLANTILLA:` resueltos borrados; nuevo `docs/planificacion/README.md` que marca las fases 0-3 como historia de la plantilla.
2. SHA: `6fe2702` en la rama `claude/template-project-init-9u4v3h`, PR [#1](https://github.com/npiobject/proyecto_2/pull/1) en borrador. `main` sigue en `732d79a`.
3. URL para probar: **backend en verde**, https://proyecto-2-npi.fly.dev/ y https://proyecto-2-npi.fly.dev/salud → `{"build":"6fe270286de3c297ac2fdd53f7a661c0755f86a2","ok":true}`, verificado por el run [34030627530](https://github.com/npiobject/proyecto_2/actions/runs/34030627530) (`workflow_dispatch` sobre la rama, tras dar de alta `FLY_API_TOKEN`). Pages, https://npiobject.github.io/proyecto_2/, **sigue sin servir**: el run [34030153868](https://github.com/npiobject/proyecto_2/actions/runs/34030153868) falló con `Create Pages site failed. Resource not accessible by integration`.
4. Drive: creada la carpeta normal `Mi unidad/proyecto_2` (id `1DQunKWKZ9JdWTCuOnQ5bOAfjIaGl9RLE`), ya anotada en `CLAUDE.md`; copia de este resumen subida a `sesiones/`.
5. Qué falta: activar Settings → Pages → Source: GitHub Actions (único bloqueo restante, solo lo puede hacer Fran) y mergear el PR #1 a `main` para que ambos workflows se relancen desde la rama por defecto.
