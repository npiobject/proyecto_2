# Sesión 2026-09-06 11:31 — inicialización desde la plantilla

1. Qué cambió: la inicialización automática no se había ejecutado (no había `.plantilla-pendiente`, pero el commit inicial llegó a `main` con el nombre `DesdeMovil` en los diez sitios `PLANTILLA:`). Sustituido a `proyecto_2` en `README.md`, `CLAUDE.md`, `app/fly.toml` + `FLY_APP` (`proyecto-2-npi`), `Cargo.toml`/`Cargo.lock`/`Dockerfile` (`proyecto-2-backend`), `tools/*.ps1` y `docs/index.html` (build `P2-B3-20260906-001`); comentarios `PLANTILLA:` resueltos borrados; nuevo `docs/planificacion/README.md` que marca las fases 0-3 como historia de la plantilla.
2. SHA: rama `claude/template-project-init-9u4v3h`, PR [#1](https://github.com/npiobject/proyecto_2/pull/1) en borrador. `main` sigue en `732d79a`.
3. URL para probar: https://npiobject.github.io/proyecto_2/ — **todavía no sirve**: el run de Pages del commit inicial ([34030153868](https://github.com/npiobject/proyecto_2/actions/runs/34030153868)) falló con `Create Pages site failed. Resource not accessible by integration`. Falta activar Settings → Pages → Source: GitHub Actions.
4. Drive: creada la carpeta normal `Mi unidad/proyecto_2` (id `1DQunKWKZ9JdWTCuOnQ5bOAfjIaGl9RLE`), ya anotada en `CLAUDE.md`; copia de este resumen subida a `sesiones/`.
5. Qué falta: activar Pages, dar de alta el secreto `FLY_API_TOKEN` (el run de Fly [34030153851](https://github.com/npiobject/proyecto_2/actions/runs/34030153851) falló por eso) y mergear el PR #1 a `main` para que ambos workflows se relancen y verifiquen.
